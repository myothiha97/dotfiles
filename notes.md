## issues
- when using terminal, if i select some previous text generated by the terminal, the ghostty terminal automatically go into the vim mode or something similar and only behave normally when i hit esc. The issue mainly occur during tmux is active 
- make sure when in tmux, almost all important feature of ghostty should also be applied correctly in tmux smoothly, currently i am seeing some behavior issues in tmux but in ghostty everything work smoothly 
